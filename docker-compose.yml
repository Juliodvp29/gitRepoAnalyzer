services:
  ghan-backend:
    image: ghan-backend
    ports:
      - "3000:3000"
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      AI_PROMPT: ${AI_PROMPT}
      AI_COMPARE_PROMPT: ${AI_COMPARE_PROMPT}